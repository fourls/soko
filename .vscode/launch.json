{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch sokod",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/sokod",
            "cwd": "${workspaceFolder}"
        }
    ]
}